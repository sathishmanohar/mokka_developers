<%= simple_form_for(@topic,
      defaults: { input_html: { class: "span8" } }) do |f| %>
  <%= f.input :title %>
  <%= f.input :body, as: "text" %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
